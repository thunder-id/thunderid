openapi: 3.0.3
info:
  title: Design Management API
  version: "1.0"
  description: Manage themes and layouts that control the visual appearance of authentication screens. Themes define color palettes and typography; layouts define the structural composition of each screen.
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html

servers:
  - url: https://{host}:{port}
    variables:
      host:
        default: "localhost"
      port:
        default: "8090"

tags:
  - name: Themes
    description: Create, update, and delete color and typography themes for login screens.
  - name: Layouts
    description: Create, update, and delete structural layout configurations for authentication screens.
  - name: design-resolve
    description: Operations for resolving design configurations

security:
  - OAuth2: [system]

paths:
  /design/themes:
    get:
      tags:
        - Themes
      summary: List themes
      description: Retrieve a list of all theme configurations.
      parameters:
        - $ref: '#/components/parameters/limitQueryParam'
        - $ref: '#/components/parameters/offsetQueryParam'
      responses:
        "200":
          description: List of theme configurations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThemeListResponse'
              example:
                totalResults: 5
                startIndex: 1
                count: 5
                themes:
                  - id: "019c4e3a-198e-7e4c-99b5-26e327b9bbfc"
                    handle: "classic"
                    displayName: "Classic"
                    description: "A timeless theme with warm accents and readable contrast for programming."
                    createdAt: ""
                    updatedAt: ""
                  - id: "019c4e3a-19ad-7159-bb2b-228692f96008"
                    handle: "high-contrast"
                    displayName: "High Contrast"
                    description: "A high contrast theme optimized for accessibility with bold colors and maximum readability."
                    createdAt: ""
                    updatedAt: ""
                links: []
        "400":
          $ref: '#/components/responses/BadRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "500":
          $ref: '#/components/responses/InternalServerError'

    post:
      tags:
        - Themes
      summary: Create theme
      description: Create a new theme configuration.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ThemeRequest'
            example:
              handle: "corporate-theme"
              displayName: "Corporate Theme"
              theme:
                primaryColor: "#1976d2"
                secondaryColor: "#dc004e"
                fontFamily: "Roboto"
                borderRadius: "4px"
      responses:
        "201":
          description: Theme created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThemeResponse'
              example:
                id: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
                handle: "corporate-theme"
                displayName: "Corporate Theme"
                theme:
                  primaryColor: "#1976d2"
                  secondaryColor: "#dc004e"
                  fontFamily: "Roboto"
                  borderRadius: "4px"
        "400":
          $ref: '#/components/responses/BadRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "500":
          $ref: '#/components/responses/InternalServerError'

  /design/themes/{id}:
    get:
      tags:
        - Themes
      summary: Get theme
      description: Retrieve a specific theme configuration by ID.
      parameters:
        - $ref: '#/components/parameters/idPathParam'
      responses:
        "200":
          description: Theme configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThemeResponse'
              example:
                id: "019c4e3a-198e-7e4c-99b5-26e327b9bbfc"
                handle: "classic"
                displayName: "Classic"
                description: "A timeless theme with warm accents and readable contrast for programming."
                theme:
                  direction: "ltr"
                  defaultColorScheme: "light"
                  colorSchemes:
                    light:
                      colors:
                        primary:
                          main: "#ff7300"
                          dark: "#cc5c00"
                          contrastText: "#ffffff"
                        secondary:
                          main: "#757575"
                          dark: "#5c5c5c"
                          contrastText: "#ffffff"
                        background:
                          default: "#fafafa"
                          paper: "#ffffff"
                        text:
                          primary: "#000000"
                          secondary: "#444444"
                    dark:
                      colors:
                        primary:
                          main: "#ff7300"
                          dark: "#cc5c00"
                          contrastText: "#ffffff"
                        secondary:
                          main: "#757575"
                          dark: "#5c5c5c"
                          contrastText: "#ffffff"
                        background:
                          default: "#121212"
                          paper: "#1e1e1e"
                        text:
                          primary: "#ffffff"
                          secondary: "#cccccc"
                  shape:
                    borderRadius: 8
                  typography:
                    fontFamily: "'Inter Variable', sans-serif"
                createdAt: ""
                updatedAt: ""
        "400":
          $ref: '#/components/responses/BadRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "404":
          description: 'Not Found: The theme could not be found'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                code: "THM-1003"
                message:
                  key: "theme.error.not_found"
                  defaultValue: "Theme not found"
                description:
                  key: "theme.error.not_found_description"
                  defaultValue: "The requested theme configuration was not found"
        "500":
          $ref: '#/components/responses/InternalServerError'

    put:
      tags:
        - Themes
      summary: Update theme
      description: Update an existing theme configuration.
      parameters:
        - $ref: '#/components/parameters/idPathParam'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ThemeRequest'
            example:
              handle: "corporate-theme"
              displayName: "Updated Corporate Theme"
              theme:
                primaryColor: "#1976d2"
                secondaryColor: "#dc004e"
                fontFamily: "Inter"
                borderRadius: "8px"
      responses:
        "200":
          description: Theme updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThemeResponse'
        "400":
          $ref: '#/components/responses/BadRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "404":
          description: 'Not Found: The theme could not be found'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                code: "THM-1003"
                message:
                  key: "theme.error.not_found"
                  defaultValue: "Theme not found"
                description:
                  key: "theme.error.not_found_description"
                  defaultValue: "The requested theme configuration was not found"
        "500":
          $ref: '#/components/responses/InternalServerError'

    delete:
      tags:
        - Themes
      summary: Delete theme
      description: Delete a theme configuration.
      parameters:
        - $ref: '#/components/parameters/idPathParam'
      responses:
        "204":
          description: Theme deleted successfully
        "400":
          $ref: '#/components/responses/BadRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "404":
          description: 'Not Found: The theme could not be found'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                code: "THM-1003"
                message:
                  key: "theme.error.not_found"
                  defaultValue: "Theme not found"
                description:
                  key: "theme.error.not_found_description"
                  defaultValue: "The requested theme configuration was not found"
        "409":
          $ref: '#/components/responses/Conflict'
        "500":
          $ref: '#/components/responses/InternalServerError'

  /design/themes/{id}/usages:
    get:
      tags:
        - Themes
      summary: Get theme usages
      description: |
        Returns the resources that reference this theme, aggregated across all
        resource types. This endpoint is informational only — it drives the
        pre-delete confirmation dialog in the UI and does not gate deletion on the server.

        Each usage entry includes a `behaviorOnDelete` field describing what happens
        to the referencing resource if the theme is deleted:
        - `fallback`: the reference is kept as-is and resolved to the system default theme at read time.
        - `cascade`: the resource is permanently deleted along with the theme.

        When usage data is unavailable (e.g. the server has not fully initialised),
        `totalResults` and `summary` are `null` rather than `0`/empty.
        Consumers must treat `null` as "unknown" and `0` as "confirmed empty".
      parameters:
        - $ref: '#/components/parameters/idPathParam'
        - $ref: '#/components/parameters/limitQueryParam'
        - $ref: '#/components/parameters/offsetQueryParam'
      responses:
        "200":
          description: Resources that reference this theme
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThemeUsagesResponse'
              example:
                totalResults: 3
                count: 3
                summary:
                  application: 2
                  agent: 1
                usages:
                  - resourceType: application
                    id: "a1b2c3d4-0000-0000-0000-000000000001"
                    displayName: "Customer Portal"
                    behaviorOnDelete: fallback
                  - resourceType: application
                    id: "a1b2c3d4-0000-0000-0000-000000000002"
                    displayName: "Admin Console"
                    behaviorOnDelete: fallback
                  - resourceType: agent
                    id: "a1b2c3d4-0000-0000-0000-000000000003"
                    displayName: "Support Agent"
                    behaviorOnDelete: fallback
        "400":
          $ref: '#/components/responses/BadRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "404":
          description: 'Not Found: The theme could not be found'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                code: "THM-1003"
                message:
                  key: "theme.error.not_found"
                  defaultValue: "Theme not found"
                description:
                  key: "theme.error.not_found_description"
                  defaultValue: "The requested theme configuration was not found"
        "500":
          $ref: '#/components/responses/InternalServerError'

  /design/layouts:
    get:
      tags:
        - Layouts
      summary: List layouts
      description: Retrieve a list of all layout configurations.
      parameters:
        - $ref: '#/components/parameters/limitQueryParam'
        - $ref: '#/components/parameters/offsetQueryParam'
      responses:
        "200":
          description: List of layout configurations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LayoutListResponse'
              example:
                totalResults: 1
                startIndex: 1
                count: 1
                layouts:
                  - id: "019c50f8-c0d6-7055-8e03-e19bac6ef649"
                    handle: "modern"
                    displayName: "Modern"
                    description: "A clean, modern authentication layout with a centered card experience, soft gradients, and balanced spacing."
                    createdAt: ""
                    updatedAt: ""
                links: []
        "400":
          $ref: '#/components/responses/BadRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "500":
          $ref: '#/components/responses/InternalServerError'

    post:
      tags:
        - Layouts
      summary: Create layout
      description: Create a new layout configuration.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LayoutRequest'
            example:
              handle: "modern-layout"
              displayName: "Modern Layout"
              layout:
                contentAlignment: "center"
                maxWidth: "1200px"
                showLogo: true
                logoPosition: "top"
      responses:
        "201":
          description: Layout created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LayoutResponse'
              example:
                id: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
                handle: "modern-layout"
                displayName: "Modern Layout"
                layout:
                  contentAlignment: "center"
                  maxWidth: "1200px"
                  showLogo: true
                  logoPosition: "top"
        "400":
          $ref: '#/components/responses/BadRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "409":
          description: 'Conflict: A layout with the same ID already exists'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                code: "LAY-1004"
                message:
                  key: "layout.error.already_exists"
                  defaultValue: "Layout already exists"
                description:
                  key: "layout.error.already_exists_description"
                  defaultValue: "A layout with the same ID already exists"
        "500":
          $ref: '#/components/responses/InternalServerError'

  /design/layouts/{id}:
    get:
      tags:
        - Layouts
      summary: Get layout
      description: Retrieve a specific layout configuration by ID.
      parameters:
        - $ref: '#/components/parameters/idPathParam'
      responses:
        "200":
          description: Layout configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LayoutResponse'
              example:
                id: "019c50f8-c0d6-7055-8e03-e19bac6ef649"
                handle: "modern"
                displayName: "Modern"
                description: "A clean, modern authentication layout with a centered card experience, soft gradients, and balanced spacing."
                layout:
                  screens:
                    auth:
                      template: "centered"
                      background:
                        type: "gradient"
                        value: "linear-gradient(135deg, #667eea 0%, #764ba2 100%)"
                      spacing:
                        componentGap: 16
                        sectionGap: 24
                      slots:
                        header:
                          enabled: true
                          height: 80
                          padding: 16
                          layout:
                            type: "flex"
                            direction: "row"
                            justify: "space-between"
                            align: "center"
                          showLogo: true
                          showLanguageSelector: true
                        main:
                          position:
                            anchor: "center"
                            verticalAlign: "center"
                          container:
                            maxWidth: 420
                            padding: 24
                            borderRadius: 12
                            elevation: 2
                            background: "surface"
                          layout:
                            type: "stack"
                            direction: "vertical"
                            gap: "md"
                            align: "stretch"
                        footer:
                          enabled: true
                          height: 60
                          padding: 16
                          layout:
                            type: "flex"
                            direction: "row"
                            justify: "center"
                            align: "center"
                          showLinks: true
                    signin:
                      extends: "auth"
                      slots:
                        main:
                          container:
                            maxWidth: 420
                    signup:
                      extends: "auth"
                      slots:
                        main:
                          container:
                            maxWidth: 520
                    mfa:
                      extends: "auth"
                      slots:
                        header:
                          showBackButton: true
                        main:
                          container:
                            maxWidth: 380
                    forgot-password:
                      extends: "auth"
                      slots:
                        main:
                          container:
                            maxWidth: 400
                    recovery:
                      extends: "auth"
                      slots:
                        main:
                          container:
                            maxWidth: 400
                    password-reset:
                      extends: "auth"
                      slots:
                        main:
                          container:
                            maxWidth: 440
                createdAt: ""
                updatedAt: ""
        "400":
          $ref: '#/components/responses/BadRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "404":
          description: 'Not Found: The layout could not be found'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                code: "LAY-1003"
                message:
                  key: "layout.error.not_found"
                  defaultValue: "Layout not found"
                description:
                  key: "layout.error.not_found_description"
                  defaultValue: "The requested layout configuration was not found"
        "500":
          $ref: '#/components/responses/InternalServerError'

    put:
      tags:
        - Layouts
      summary: Update layout
      description: Update an existing layout configuration.
      parameters:
        - $ref: '#/components/parameters/idPathParam'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LayoutRequest'
            example:
              handle: "modern-layout"
              displayName: "Updated Modern Layout"
              layout:
                contentAlignment: "left"
                maxWidth: "1400px"
                showLogo: true
                logoPosition: "left"
      responses:
        "200":
          description: Layout updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LayoutResponse'
        "400":
          $ref: '#/components/responses/BadRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "404":
          description: 'Not Found: The layout could not be found'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                code: "LAY-1003"
                message:
                  key: "layout.error.not_found"
                  defaultValue: "Layout not found"
                description:
                  key: "layout.error.not_found_description"
                  defaultValue: "The requested layout configuration was not found"
        "500":
          $ref: '#/components/responses/InternalServerError'

    delete:
      tags:
        - Layouts
      summary: Delete layout
      description: Delete a layout configuration.
      parameters:
        - $ref: '#/components/parameters/idPathParam'
      responses:
        "204":
          description: Layout deleted successfully
        "400":
          $ref: '#/components/responses/BadRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "404":
          description: 'Not Found: The layout could not be found'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                code: "LAY-1003"
                message:
                  key: "layout.error.not_found"
                  defaultValue: "Layout not found"
                description:
                  key: "layout.error.not_found_description"
                  defaultValue: "The requested layout configuration was not found"
        "500":
          $ref: '#/components/responses/InternalServerError'

  /design/resolve:
    get:
      tags:
        - design-resolve
      summary: Resolve design configuration
      description: |
        Resolves and retrieves the complete design configuration (theme + layout) for a given entity.
        This endpoint merges theme and layout preferences, with layout preferences taking precedence over theme preferences when there are conflicts.
      security: []
      parameters:
        - name: type
          in: query
          description: The type of entity to resolve design for
          required: true
          schema:
            type: string
            enum: [APP, OU]
          example: APP
        - name: id
          in: query
          description: The ID of the entity (application ID or organization unit ID)
          required: true
          schema:
            type: string
            format: uuid
          example: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
      responses:
        "200":
          description: Resolved design configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DesignResolveResponse'
              example:
                theme:
                  direction: "ltr"
                  defaultColorScheme: "dark"
                  colorSchemes:
                    dark:
                      palette:
                        primary:
                          main: "#BD93F9"
                layout:
                  header:
                    logo:
                      url: "https://example.com/logo.png"
        "400":
          $ref: '#/components/responses/BadRequest'
        "404":
          description: Entity not found or has no design configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                code: "DSR-1004"
                message:
                  key: "design.resolve.error.app_not_found"
                  defaultValue: "Application not found"
                description:
                  key: "design.resolve.error.app_not_found_description"
                  defaultValue: "The application with the specified id does not exist"
        "500":
          $ref: '#/components/responses/InternalServerError'

components:
  securitySchemes:
    OAuth2:
      type: oauth2
      flows:
        clientCredentials:
          tokenUrl: /oauth2/token
          scopes:
            system: System level access

  parameters:
    idPathParam:
      name: id
      in: path
      description: The unique identifier
      required: true
      schema:
        type: string
        format: uuid

    limitQueryParam:
      name: limit
      in: query
      description: Maximum number of results to return
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 10

    offsetQueryParam:
      name: offset
      in: query
      description: Number of results to skip
      required: false
      schema:
        type: integer
        minimum: 0
        default: 0

  schemas:
    ThemeRequest:
      type: object
      required:
        - handle
        - displayName
        - theme
      properties:
        handle:
          type: string
          description: Unique kebab-case identifier for the theme
          example: "corporate-theme"
        displayName:
          type: string
          description: Display name for the theme
          example: "Corporate Theme"
        description:
          type: string
          description: Description of the theme
          example: "Dracula theme..."
        theme:
          type: object
          description: Theme configuration as a JSON object
          additionalProperties: true
          example:
            primaryColor: "#1976d2"
            secondaryColor: "#dc004e"
            fontFamily: "Roboto"
            borderRadius: "4px"

    ThemeResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Unique identifier for the theme
          example: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
        handle:
          type: string
          description: Unique kebab-case identifier for the theme
          example: "corporate-theme"
        displayName:
          type: string
          description: Display name for the theme
          example: "Corporate Theme"
        description:
          type: string
          description: Description of the theme
          example: "Dracula theme..."
        theme:
          type: object
          description: Theme configuration as a JSON object
          additionalProperties: true
          example:
            primaryColor: "#1976d2"
            secondaryColor: "#dc004e"
            fontFamily: "Roboto"
            borderRadius: "4px"
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the theme was created (ISO 8601 format)
          example: "2024-01-15T10:30:00Z"
        updatedAt:
          type: string
          format: date-time
          description: Timestamp when the theme was last updated (ISO 8601 format)
          example: "2024-01-20T14:45:00Z"
        isReadOnly:
          type: boolean
          description: Indicates whether the theme is read-only (declarative)
          example: false

    ThemeListItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Unique identifier for the theme
          example: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
        handle:
          type: string
          description: Unique kebab-case identifier for the theme
          example: "corporate-theme"
        displayName:
          type: string
          description: Display name for the theme
          example: "Corporate Theme"
        description:
          type: string
          description: Description of the theme
          example: "A timeless theme with warm accents."
        defaultColorScheme:
          type: string
          description: The name of the default color scheme for this theme (e.g. "light" or "dark")
          example: "light"
        primaryColor:
          type: string
          description: The primary color hex value from the default color scheme's palette
          example: "#ff7300"
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the theme was created (ISO 8601 format)
          example: "2024-01-15T10:30:00Z"
        updatedAt:
          type: string
          format: date-time
          description: Timestamp when the theme was last updated (ISO 8601 format)
          example: "2024-01-20T14:45:00Z"
        isReadOnly:
          type: boolean
          description: Indicates whether the theme is read-only (declarative)
          example: false

    ThemeListResponse:
      type: object
      properties:
        totalResults:
          type: integer
          description: Total number of themes
          example: 10
        startIndex:
          type: integer
          description: Index of the first result
          example: 1
        count:
          type: integer
          description: Number of results in this response
          example: 10
        links:
          type: array
          description: Pagination links
          items:
            type: object
            properties:
              href:
                type: string
                description: URL for this link
                example: "/design/themes?limit=10&offset=10"
              rel:
                type: string
                description: Link relation type
                example: "next"
        themes:
          type: array
          items:
            $ref: '#/components/schemas/ThemeListItem'

    ThemeUsage:
      type: object
      description: A single resource that references the theme.
      required:
        - resourceType
        - id
        - displayName
        - behaviorOnDelete
      properties:
        resourceType:
          type: string
          description: Type of the referencing resource.
          enum: [application, agent]
          example: application
        id:
          type: string
          format: uuid
          description: Identifier of the referencing resource.
          example: "a1b2c3d4-0000-0000-0000-000000000001"
        displayName:
          type: string
          description: Human-readable name of the referencing resource.
          example: "Customer Portal"
        behaviorOnDelete:
          type: string
          description: |
            What happens to this resource when the theme is deleted.
            - `fallback`: the reference is kept and resolved to the system default theme at read time.
            - `cascade`: the resource is permanently deleted.
          enum: [fallback, cascade]
          example: fallback

    ThemeUsagesSummary:
      type: object
      nullable: true
      description: |
        Per-resource-type count of usages, keyed by resource type
        (e.g. `application`, `agent`). Null when the counts could not be determined.
      additionalProperties:
        type: integer
      example:
        application: 2
        agent: 1

    ThemeUsagesResponse:
      type: object
      properties:
        totalResults:
          type: integer
          nullable: true
          description: |
            Total number of resources that reference this theme.
            Null when usage data is unavailable; 0 means confirmed empty.
          example: 3
        count:
          type: integer
          description: Number of results returned.
          example: 3
        summary:
          $ref: '#/components/schemas/ThemeUsagesSummary'
        usages:
          type: array
          items:
            $ref: '#/components/schemas/ThemeUsage'

    LayoutRequest:
      type: object
      required:
        - handle
        - displayName
        - layout
      properties:
        handle:
          type: string
          description: Unique kebab-case identifier for the layout
          example: "modern-layout"
        displayName:
          type: string
          description: Display name for the layout
          example: "Modern Layout"
        description:
          type: string
          description: Description of the layout
          example: "A clean, modern authentication layout"
        layout:
          type: object
          description: Layout configuration as a JSON object
          additionalProperties: true
          example:
            contentAlignment: "center"
            maxWidth: "1200px"
            showLogo: true
            logoPosition: "top"

    LayoutResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Unique identifier for the layout
          example: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
        handle:
          type: string
          description: Unique kebab-case identifier for the layout
          example: "modern-layout"
        displayName:
          type: string
          description: Display name for the layout
          example: "Modern Layout"
        description:
          type: string
          description: Description of the layout
          example: "A clean, modern authentication layout"
        layout:
          type: object
          description: Layout configuration as a JSON object
          additionalProperties: true
          example:
            contentAlignment: "center"
            maxWidth: "1200px"
            showLogo: true
            logoPosition: "top"
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the layout was created (ISO 8601 format)
          example: "2024-01-15T10:30:00Z"
        updatedAt:
          type: string
          format: date-time
          description: Timestamp when the layout was last updated (ISO 8601 format)
          example: "2024-01-20T14:45:00Z"
        isReadOnly:
          type: boolean
          description: Indicates whether the layout is read-only (declarative)
          example: false

    LayoutListItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Unique identifier for the layout
          example: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
        handle:
          type: string
          description: Unique kebab-case identifier for the layout
          example: "modern-layout"
        displayName:
          type: string
          description: Display name for the layout
          example: "Modern Layout"
        description:
          type: string
          description: Description of the layout
          example: "A clean, modern authentication layout"
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the layout was created (ISO 8601 format)
          example: "2024-01-15T10:30:00Z"
        updatedAt:
          type: string
          format: date-time
          description: Timestamp when the layout was last updated (ISO 8601 format)
          example: "2024-01-20T14:45:00Z"
        isReadOnly:
          type: boolean
          description: Indicates whether the layout is read-only (declarative)
          example: false

    LayoutListResponse:
      type: object
      properties:
        totalResults:
          type: integer
          description: Total number of layouts
          example: 5
        startIndex:
          type: integer
          description: Index of the first result
          example: 1
        count:
          type: integer
          description: Number of results in this response
          example: 5
        links:
          type: array
          description: Pagination links
          items:
            type: object
            properties:
              href:
                type: string
                description: URL for this link
                example: "/design/layouts?limit=10&offset=10"
              rel:
                type: string
                description: Link relation type
                example: "next"
        layouts:
          type: array
          items:
            $ref: '#/components/schemas/LayoutListItem'

    DesignResolveResponse:
      type: object
      properties:
        theme:
          type: object
          description: Resolved theme configuration as a JSON object
          additionalProperties: true
        layout:
          type: object
          description: Resolved layout configuration as a JSON object
          additionalProperties: true

    Error:
      type: object
      properties:
        code:
          type: string
          description: "Error code. Prefixes: THM-XXXX (theme), LAY-XXXX (layout), DSR-XXXX (design resolve)."
          example: "THM-1003"
        message:
          $ref: '#/components/schemas/I18nMessage'
        description:
          $ref: '#/components/schemas/I18nMessage'

    I18nMessage:
      type: object
      description: Internationalized message with translation key and default value.
      required:
        - key
        - defaultValue
      properties:
        key:
          type: string
          description: Translation key for fetching localized message.
        defaultValue:
          type: string
          description: Default message in English (fallback).

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            code: "DSR-1001"
            message:
              key: "error.designservice.invalid_request"
              defaultValue: "Invalid request format"
            description:
              key: "error.designservice.invalid_request_description"
              defaultValue: "The request contains invalid parameters"

    Unauthorized:
      description: Unauthorized - missing or invalid authentication token
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            code: "AUTH-4010"
            message:
              key: "error.unauthorized"
              defaultValue: "Unauthorized"
            description:
              key: "error.unauthorized_description"
              defaultValue: "Authentication is required to access this resource"

    Conflict:
      description: Conflict - resource cannot be deleted as it's in use
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            code: "THM-1004"
            message:
              key: "theme.error.in_use"
              defaultValue: "Theme in use"
            description:
              key: "theme.error.in_use_description"
              defaultValue: "Cannot delete theme that is currently associated with one or more applications"

    InternalServerError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            code: "SSE-5000"
            message:
              key: "error.internal_server_error"
              defaultValue: "Internal server error"
            description:
              key: "error.internal_server_error_description"
              defaultValue: "An unexpected error occurred while processing the request"
