---
title: Try It Out
description: Run the AI agent use cases against the Wayfinder sample, a fictional travel-booking app with a built-in AI chat assistant.
sidebar_position: 5
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Stepper from '../../../src/components/Stepper';
import {NextSteps, NextStepsCard} from '../../../src/components/NextSteps';
import SampleDownload from '../../../src/components/SampleDownload';
import { WayfinderAgentOrganization, WayfinderAgentArchitecture } from '../../../src/components/WayfinderDiagrams';

# Try It Out

Each walkthrough below runs one of the agent identity patterns from [Securing AI Agents](../overview) against the Wayfinder sample.

## Meet Wayfinder

Wayfinder is a travel-booking application with an AI agent built in — the **Wayfinder Concierge**, an in-app chat assistant. Through Wayfinder, consumers search for flights and hotels, book trips, and ask the Concierge to do the same on their behalf.

<WayfinderAgentOrganization />

### Meet the Cast

- **Consumers** book travel and chat with the agent.
  - **John Doe** is the customer with full access. He can book through the UI *and* talk to the Wayfinder Concierge. He carries both the `Booking User` and `Chat User` roles.
  - **Jane Smith** also books through the UI, but does not have access to the Wayfinder Concierge. She carries only the `Booking User` role.
- **The Wayfinder Concierge** is a first-class principal in <ProductName /> with its own credentials. It uses its own identity for browsing tools, and switches to a user-context token when a tool needs the consumer's consent.

## Sample Architecture

Wayfinder runs as four deployable pieces:

- **Wayfinder Web**: the browser-based app that hosts the chat widget.
- **AI Agent**: drives the conversation and calls Wayfinder Server tools.
- **Wayfinder Server**: holds the booking data and exposes it over both REST (`/api/*`) and MCP (`/mcp`).
- **<ProductName />**: authenticates users and the agent, issues the tokens.

<WayfinderAgentArchitecture />

## Set Up Your Environment

<Stepper stepNode="h3" as="h4">

### Run <ProductName />

Get <ProductName /> running locally. Follow [Get <ProductName />](/docs/next/guides/getting-started/get-thunderid) for download, setup, and start commands.

### Configure the Server

Edit `repository/conf/deployment.yaml` and add the Wayfinder origin under `cors.allowed_origins`. Leave any existing entries in place.

```yaml
cors:
  allowed_origins:
    # ...existing entries...
    - "{{WayFinderSampleUrl}}"
```

Restart <ProductName /> after the change.

### Get the Wayfinder Sample

Download the latest Wayfinder sample distribution. It ships with a `thunderid-config/` directory containing a declarative YAML bundle and a `thunderid.env` file with the environment variables it references, plus the Wayfinder web frontend, the AI Agent service, and the Wayfinder Server.

<SampleDownload sample="sample-app-wayfinder" />

### Configure the Sample

The `backend/`, `ai-agent/`, and `frontend/` folders each ship with a `.env` file. The defaults already point at <ProductName /> on `https://localhost:8090`. The only placeholder you must fill in is the LLM API key in `ai-agent/.env` — an Anthropic API key from [console.anthropic.com](https://console.anthropic.com), or a Google Gemini API key from [aistudio.google.com](https://aistudio.google.com).

### Import the Sample Bundle

Apply the bundle through the <ProductName /> Console. It creates everything the walkthroughs share — including the Wayfinder Concierge agent.

1. Sign in to the <ProductName /> Console at <ConsoleUrl />.
2. On first sign-in, a welcome screen appears with an **Open** button. (Later, reach the same screen from the user profile menu in the top-right corner of the Console.)
3. Click **Open** and select your `thunderid-config/thunderid-config.yaml` file from the sample distribution.
4. Select your `thunderid-config/thunderid.env` file to provide the environment variables referenced in the YAML.
5. The Console imports the files and reports the resources it created when the import completes.

### Run the Sample

Start all Wayfinder services from the extracted sample directory:

<Tabs groupId="os">
<TabItem value="unix" label="Linux / macOS">

```bash
./start.sh
```

</TabItem>
<TabItem value="windows" label="Windows">

```powershell
.\start.ps1
```

</TabItem>
</Tabs>

The sample opens at <WayFinderSampleUrl />.

</Stepper>

## Walkthroughs

Pick a walkthrough to begin. Each one starts from the setup above.

<NextSteps>
  <NextStepsCard
    title="Protect the Agent"
    description="John gets in to the Wayfinder Concierge; Jane is rejected."
    href="./protect-the-agent"
  />
  <NextStepsCard
    title="Acting on Its Own"
    description="The Concierge uses its own M2M token to call browsing tools."
    href="./act-on-its-own"
  />
  <NextStepsCard
    title="Acting on Behalf of a User"
    description="The Concierge pauses for John's consent and books on his behalf."
    href="./act-on-behalf-of-user"
  />
</NextSteps>

## Going Deeper

- Curious how the bundle maps to <ProductName /> concepts? See [Identity Concepts](../identity-concepts).
- Prefer to build the same setup manually instead of importing the bundle? See [Configure It Yourself](../configure-it-yourself).
