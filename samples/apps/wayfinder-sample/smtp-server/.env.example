# Wayfinder local SMTP server configuration.

# SMTP listener — where ThunderID delivers outgoing mail.
SMTP_HOST=127.0.0.1
SMTP_PORT=2525

# Web inbox UI and its JSON API.
HTTP_HOST=127.0.0.1
HTTP_PORT=8788

# Maximum number of emails kept in memory (oldest are dropped first).
MAX_MESSAGES=500
