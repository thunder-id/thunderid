---
title: <SignInButton />
docType: reference
description: Trigger the sign-in authentication flow from a pre-styled button component in Nuxt applications.
---

# `<SignInButton />`

The `SignInButton` component initiates the sign-in flow when clicked. It is auto-registered by the Nuxt module and requires no imports.

## Usage

### Basic Usage

```vue title="pages/index.vue" showLineNumbers
<template>
  <SignInButton />
</template>
```

### Custom Label

Pass text content to override the default button label:

```vue title="pages/index.vue" showLineNumbers
<template>
  <SignInButton>Log in to your account</SignInButton>
</template>
```

### Custom Styling

```vue title="pages/index.vue" showLineNumbers
<template>
  <SignInButton class="btn btn-primary">
    Sign In
  </SignInButton>
</template>
```

### Using the Default Slot for Full Control

Access the `signIn` function and `isLoading` state via the default scoped slot to render a fully custom button:

```vue title="components/CustomSignInButton.vue" showLineNumbers
<template>
  <SignInButton>
    <template #default="{ signIn, isLoading }">
      <button
        :disabled="isLoading"
        class="px-4 py-2 bg-blue-600 text-white rounded"
        @click="signIn"
      >
        {{ isLoading ? 'Signing in...' : 'Sign In' }}
      </button>
    </template>
  </SignInButton>
</template>
```

## Slots

| Slot | Slot Props | Description |
| :--- | :--- | :--- |
| `default` | `{ signIn: () => void, isLoading: boolean }` | Default content rendered inside the button. If omitted, renders a default "Sign In" button |

## Props

| Prop | Type | Required | Description |
| :--- | :--- | :---: | :--- |
| `preferences` | `Preferences` | ❌ | Customization options for i18n and theming |
