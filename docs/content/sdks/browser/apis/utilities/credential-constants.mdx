---
title: CredentialConstants
docType: reference
description: Well-known credential attribute names, for referring to a credential without a string literal.
---

# `CredentialConstants`

`CredentialConstants` is a constants object exposing well-known credential attribute names, so you can refer to a credential without a string literal.

## Usage

```js
import { CredentialConstants, updateMeCredentials } from '@thunderid/browser'

await updateMeCredentials({
  baseUrl,
  payload: { [CredentialConstants.PASSWORD]: newValue },
  fetcher,
})
```

## Constants

| Constant | Type | Value | Description |
| :--- | :--- | :--- | :--- |
| `PASSWORD` | `string` | `'password'` | The default credential every user type ships with. |

## Related

- [`updateMeCredentials()`](../update-me-credentials): Writes the credential these constants name
- [`supportsCredential()`](../supports-credential): Check whether an account's type declares one of these
