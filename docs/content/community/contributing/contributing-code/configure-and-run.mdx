---
title: Configure and Run
description: Learn how to build from source, configure the server, and run the backend and frontend together.
sidebar_position: 2
hide_table_of_contents: false
---

# Configure and Run

## Build from Source

Building from source produces a distributable `.zip` file at `PROJECT_ROOT/target/dist/`.

<CodeGroup>
  <CodeBlock lang="bash" label="Linux/macOS">
    make build
  </CodeBlock>
  <CodeBlock lang="powershell" label="Windows">
    .\build.ps1 build
  </CodeBlock>
</CodeGroup>

**What this does:**

- Compiles the Go backend binary
- Initializes SQLite databases (config, runtime, user)
- Builds the frontend React apps (<ProductName /> Gate + <ProductName /> Console)
- Builds the documentation
- Builds sample apps

## Configure the Server

Open `backend/cmd/server/deployment.yaml` in your favorite text editor and make the following changes:

**Add CORS allowed origins:**

```yaml
cors:
  allowed_origins:
    - "https://localhost:5190"
    - "https://localhost:5191"
```

:::tip
This configuration allows the <ProductName /> Gate and <ProductName /> Console applications to communicate with the backend server during development.
:::

**Configure the Gate client:**

```yaml
gate_client:
  port: 5190
```

:::tip
This configuration points the backend server to the local <ProductName /> Gate application for authentication.
:::

## Run the Server

### Option A: All-in-one (Backend + Frontend together)

Start the <ProductName /> server and apps in development mode.

<CodeGroup>
  <CodeBlock lang="bash" label="Linux/macOS">
    make run
  </CodeBlock>
  <CodeBlock lang="powershell" label="Windows">
    .\build.ps1 run
  </CodeBlock>
</CodeGroup>

:::note
This command will automatically set up your complete development environment:

**What happens:**

- Installs frontend dependencies and builds packages
- Starts the frontend development servers
- Generates TLS certificates (if missing)
- Initializes SQLite databases
- Starts the backend server
- Runs the bootstrap script which creates:
  - The `Console` application with the correct redirect URIs
  - The default admin user (credentials default to `admin` / `admin`; configurable via `ADMIN_USERNAME` / `ADMIN_PASSWORD` environment variables)

**Services that start:**

- **Backend server**: [https://localhost:8090](https://localhost:8090)
- **<ProductName /> Gate** (Login/Register): [https://localhost:5190/gate](https://localhost:5190/gate)
- **<ProductName /> Console** (Admin Console): [https://localhost:5191/console](https://localhost:5191/console)
:::

### Option B: Separate terminals (for debugging independently)

Use this approach when you need to debug the backend and frontend independently.

**Terminal 1 - Backend:**

<CodeGroup>
  <CodeBlock lang="bash" label="Linux/macOS">
    make run_backend
  </CodeBlock>
  <CodeBlock lang="powershell" label="Windows">
    .\build.ps1 run_backend
  </CodeBlock>
</CodeGroup>

**Terminal 2 - Frontend:**

<CodeGroup>
  <CodeBlock lang="bash" label="Linux/macOS">
    make run_frontend
  </CodeBlock>
  <CodeBlock lang="powershell" label="Windows">
    .\build.ps1 run_frontend
  </CodeBlock>
</CodeGroup>

**Terminal 3 - Seed the initial data (one-time only):**

<CodeGroup>
  <CodeBlock lang="bash" label="Linux/macOS">
    {`PUBLIC_URL="https://localhost:8090" \\
  go run -C backend/cmd/server . bootstrap \\
  --console-redirect-uris "https://localhost:5191/console"`}
  </CodeBlock>
  <CodeBlock lang="powershell" label="Windows">
    {`$env:PUBLIC_URL = "https://localhost:8090"
go run -C backend/cmd/server . bootstrap \`
  --console-redirect-uris "https://localhost:5191/console"`}
  </CodeBlock>
</CodeGroup>

This runs the in-process `bootstrap` subcommand, which creates the default resources (including the `Console` application and the default admin user — credentials default to `admin` / `admin`) directly through the service layer. No temporary server or security bypass is involved. You only need to do this once — the data persists in the SQLite databases, and the subcommand is idempotent if re-run.

:::warning
Don't use `./setup.sh` here. That script is designed for the packaged distribution zip and expects a compiled `./thunderid` binary at the project root. From source code, run the `bootstrap` subcommand as shown above, or use `make run` (Option A) which handles everything for you.
:::

## Setup and Data Seeding

<ProductName /> provides two different data seeding paths, depending on how the system is initialized.

| Command | Use Case | Seeded Data |
|---------|----------|-------------|
| `make run` | Source development | Default resources |
| `./setup.sh` | Deployment / release initialization (seed only) | Default resources |
| `./start.sh --bootstrap-and-serve` | Local/dev: seed once, then serve | Default resources |

Both paths run the same in-process `bootstrap` subcommand, which creates the core resources through the service layer (no temporary server, no security bypass):

- Default Organization Unit
- Default User Type (Person) and Agent Type
- Admin user (credentials default to `admin` / `admin`; configurable via `ADMIN_USERNAME` / `ADMIN_PASSWORD` environment variables)
- System resource server with system resources, actions, and permissions
- Administrators group, Administrator role, and the role assignment
- `Console` application
- Default flows, themes, and translations

### Development Seeding (`make run`)

For development environments, `make run` runs `go run … bootstrap` against the local SQLite databases before starting the server.

### Full Bootstrap (`./setup.sh`)

For release artifacts and deployment environments, `./setup.sh` performs a one-time initialization. It delegates to `./start.sh --bootstrap`, which runs the compiled binary's in-process `bootstrap` one-shot (creating the resources above, then exiting) and starts the bundled consent server for the duration of the run. The bootstrap reads the resource definitions from `bootstrap/`.

### Seed and Serve in One Step (`./start.sh --bootstrap-and-serve`)

For local and development runs of a built distribution, `./start.sh --bootstrap-and-serve` runs the in-process `bootstrap` one-shot, then starts the long-running server. The bootstrap is idempotent (it skips resources that already exist), and the server reuses the consent server it started. `./start.sh --bootstrap` is the seed-only variant (used internally by `setup.sh`).

**In short:**
- Use `make run` when running <ProductName /> from source.
- Use `./setup.sh` when initializing a fresh runtime environment as a separate one-time step (e.g. a deployment init job), then `./start.sh` to serve.
- Use `./start.sh --bootstrap-and-serve` to seed and serve a built distribution in a single command (local/dev).

## Advanced Setup (Manual Mode)

<details>
<summary><strong>Click to expand - Manual Frontend Setup</strong></summary>

For developers who want to run frontend components separately without using `make run` commands.

### Installing Dependencies

1. Navigate to the <ProductName /> frontend directory.

```bash
cd frontend
```

2. Install the dependencies using `pnpm`.

```bash
pnpm install
```

### Building the Project

Execute the build command to compile the project. This will build all the necessary packages and applications.

```bash
pnpm build
```

### Seed Data (Optional)

**Note**: This step is only necessary if you are running the backend server manually and have not yet set up the initial data.

If you have not already created the `Console` application and the default admin user, you can do so by running the following command:

```bash
PUBLIC_URL="https://localhost:8090" \
  go run -C backend/cmd/server . bootstrap \
  --console-redirect-uris "https://localhost:5191/console"
```

### Setting up the <ProductName /> Gate Application

1. Point the `gate_client` in `deployment.yaml` to the local <ProductName /> Gate application.

   - If you are running `make run`, change the `gate_client` section in `backend/cmd/server/deployment.yaml`
   - If you are running the backend server manually, change the `gate_client` section in `<THUNDERID_HOME>/deployment.yaml`

```yaml
gate_client:
  port: 5190
```

2. Add the local development origin of the <ProductName /> Gate application (https://localhost:5190) to the CORS allowed origins in `<THUNDERID_HOME>/deployment.yaml`.

```yaml
cors:
  allowed_origins:
    - "https://localhost:5190"
```

3. Run the <ProductName /> Gate application.

```bash
cd frontend
pnpm --filter @thunderid/gate dev
```

### Setting up the <ProductName /> Console Application

**IMPORTANT**: This section assumes that you have already created the `Console` application using the initial data setup script. If not, please refer to the [Seed Data](#seed-data-optional) section above.

1. First, retrieve the application ID of the **Console** application from the <ProductName /> server. This will be the application with the `client_id` **CONSOLE**.

```bash
curl -k -X GET "https://localhost:8090/applications"
```

2. Then, get the current **Console** application configuration:

```bash
curl -k -X GET "https://localhost:8090/applications/<console-application-id>"
```

**Note**: Replace `<console-application-id>` with the actual application ID (e.g., `6100bc91-ba99-4ce9-87dd-6d4d80178c38`) obtained from the previous step. The `-k` flag allows curl to work with self-signed SSL certificates in development.

3. Copy the response from step 2 and update the `redirectUris` in the JSON object to include the local development URL (ex: https://localhost:5191/console). Locate the `inboundAuthConfig > config` section and update the `redirectUris` array:

```json
"redirectUris": [
  "https://localhost:8090/console",
  "https://localhost:5191/console"
]
```

4. Update the **Console** application with the modified configuration by passing the updated JSON directly:

```bash
curl -k -X PUT "https://localhost:8090/applications/<console-application-id>" \
  -H "Content-Type: application/json" \
  -d '<paste-the-modified-json-here>'
```

5. Add the local development origin of the console application (https://localhost:5191) to the CORS allowed origins in `<THUNDERID_HOME>/deployment.yaml`.

```yaml
cors:
  allowed_origins:
    - "https://localhost:5191"
```

6. Run the <ProductName /> Console application.

```bash
pnpm --filter @thunderid/console dev
```

This will run the <ProductName /> Console application on `https://localhost:5191/console`.

</details>
